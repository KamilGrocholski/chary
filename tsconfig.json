{
  "compilerOptions": {
    // Enable latest features
    "lib": ["ESNext", "DOM"],
    "target": "ESNext",
    "module": "ESNext",
    "moduleDetection": "force",
    "jsx": "react-jsx",
    "allowJs": true,

    // Every import is written from the repository root — a path reads the same wherever it
    // appears, and moving a file does not rewrite its neighbours' imports. AGENTS.md §9.3.
    "baseUrl": ".",
    "paths": { "@/*": ["./*"] },

    // Bundler mode
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,

    // Best practices
    "strict": true,
    "skipLibCheck": true,
    "noFallthroughCasesInSwitch": true,

    // Dead code is not to accumulate — NEVER_ONLINE_DAYS survived a format rebuild in
    // public/app.js only because nobody was holding it.
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    // public/app.js is not checked (no "checkJs") — under strict it produces 25 DOM typing
    // errors rather than real bugs. Worth revisiting with JSDoc annotations.
    "noPropertyAccessFromIndexSignature": false
  }
}
